{
  "name": "college-admission-system",
  "version": "1.0.0",
  "description": "Full-Stack College Admission Management Desktop Software",
  "scripts": {
    "install:all": "cmd /c \"npm install --prefix backend && npm install --prefix frontend\"",
    "dev": "node dev.js",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "start": "node backend/server.js",
    "seed": "npm run seed --prefix backend",
    "package:exe": "cmd /c \"npm run build:frontend && npx pkg backend/package.json --out-path dist\""
  }
}
